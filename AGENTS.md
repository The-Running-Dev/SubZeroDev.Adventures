# Agent Instructions

This file exists for tools that look for `AGENTS.md` by convention. It is a pointer, not a
copy — the instructions live in [`CLAUDE.md`](CLAUDE.md). Duplicating them here would give
the repository two answers to every question, the failure mode `CLAUDE.md` itself documents
for this project's coupling to its engine submodule.

Read [`CLAUDE.md`](CLAUDE.md). Nothing in it is specific to one assistant.
